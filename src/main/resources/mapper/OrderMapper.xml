<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myshop.mapper.OrderMapper">
 <insert id="insertOrder">INSERT INTO orders(user_id,order_number,status,total_amount) VALUES(#{userId},#{orderNumber},'PAID',#{total})</insert>
 <select id="findIdByNumber" resultType="long">SELECT id FROM orders WHERE order_number=#{orderNumber}</select>
 <insert id="insertItem">INSERT INTO order_items(order_id,product_id,product_name,unit_price,quantity) VALUES(#{orderId},#{line.productId},#{line.name},#{line.price},#{line.quantity})</insert>
 <update id="decreaseStock">UPDATE products SET stock=stock-#{quantity} WHERE id=#{productId} AND active=TRUE AND stock &gt;= #{quantity}</update>
 <select id="findByUser" resultType="OrderSummary">SELECT id,order_number,status,total_amount,created_at FROM orders WHERE user_id=#{userId} ORDER BY created_at DESC</select>
</mapper>
